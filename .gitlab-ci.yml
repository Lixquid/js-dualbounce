image: node

cache:
    paths:
        - node_modules/

build:
    stage: build
    script:
        - yarn build

test:
    stage: test
    script:
        - yarn
        - yarn test
        - "yarn lint:prettier"
        - "yarn lint:eslint"
